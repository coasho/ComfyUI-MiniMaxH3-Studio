# 核心节点只用 ComfyUI 自带的 torch / torchaudio / requests / aiohttp，不需要额外装东西。
# 下面这些只在编辑器的可选功能用到，装不上也只是那个功能不可用，节点本身照常。

# 图生文反推（Qwen3-VL-4B）。Qwen3VL 是 4.57 开始原生支持的，低于这个版本会
# 报 KeyError: 'qwen3_vl'，而不是提示你版本太低。
transformers>=4.57.0

# 图生文反推的二次元标签模型（wd-eva02-large-tagger-v3，ONNX）
onnxruntime>=1.17.0

# 音色落盘。torchaudio.save 在 2.9 起要 torchcodec，没有就走 soundfile。
soundfile>=0.12.0

# 反推要缩图
pillow>=10.0.0
